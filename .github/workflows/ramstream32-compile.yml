name: ramstream32 CUDA compile

on:
  push:
    paths:
      - 'src/cuda/gridfp/oneesan_cuda_gridfp_ramstream32_factorized_forced2.cu'
      - 'src/cuda/gridfp/oneesan_cuda_gridfp_ramstream32_factorized_direct2d.cu'
      - 'src/cuda/gridfp/oneesan_cuda_gridfp_ramstream32_factorized_lowdesc.cu'
      - 'src/cuda/gridfp/oneesan_cuda_gridfp_ramstream32_factorized_bidesc.cu'
      - 'src/cuda/gridfp/ramstream32_factorized_storage.hpp'
      - 'src/cuda/gridfp/ramstream32_lowdesc.cuh'
      - 'src/cuda/gridfp/ramstream32_highdesc.cuh'
      - 'src/cuda/b300/oneesan_cuda_gridfp_b300_hbm32_factorized_batch.cu'
      - 'src/cuda/common/**'
      - 'scripts/build/gridfp-ramstream32-factorized*.sh'
      - '.github/workflows/ramstream32-compile.yml'
  pull_request:
    paths:
      - 'src/cuda/gridfp/oneesan_cuda_gridfp_ramstream32_factorized_forced2.cu'
      - 'src/cuda/gridfp/oneesan_cuda_gridfp_ramstream32_factorized_direct2d.cu'
      - 'src/cuda/gridfp/oneesan_cuda_gridfp_ramstream32_factorized_lowdesc.cu'
      - 'src/cuda/gridfp/oneesan_cuda_gridfp_ramstream32_factorized_bidesc.cu'
      - 'src/cuda/gridfp/ramstream32_factorized_storage.hpp'
      - 'src/cuda/gridfp/ramstream32_lowdesc.cuh'
      - 'src/cuda/gridfp/ramstream32_highdesc.cuh'
      - 'src/cuda/b300/oneesan_cuda_gridfp_b300_hbm32_factorized_batch.cu'
      - 'src/cuda/common/**'
      - 'scripts/build/gridfp-ramstream32-factorized*.sh'
      - '.github/workflows/ramstream32-compile.yml'
  workflow_dispatch:

jobs:
  compile:
    runs-on: ubuntu-latest
    container: nvidia/cuda:12.8.1-devel-ubuntu22.04
    strategy:
      matrix:
        include:
          - width: 22
            n: 21
            low: 11
            high: 10
          - width: 28
            n: 27
            low: 14
            high: 13
    steps:
      - uses: actions/checkout@v4
      - name: Compile forced2 factorized RAM backend
        shell: bash
        run: |
          mkdir -p build
          nvcc -O3 -std=c++17 -lineinfo -arch=sm_80 \
            -DTARGET_W=${{ matrix.width }} \
            -DLOW_LUT_K=${{ matrix.low }} \
            -DHIGH_LUT_K=${{ matrix.high }} \
            src/cuda/gridfp/oneesan_cuda_gridfp_ramstream32_factorized_forced2.cu \
            -o build/ramstream32_forced2_w${{ matrix.width }}
      - name: Compile direct2d factorized RAM backend
        shell: bash
        run: |
          nvcc -O3 -std=c++17 -lineinfo -arch=sm_80 \
            -DTARGET_W=${{ matrix.width }} \
            -DLOW_LUT_K=${{ matrix.low }} \
            -DHIGH_LUT_K=${{ matrix.high }} \
            src/cuda/gridfp/oneesan_cuda_gridfp_ramstream32_factorized_direct2d.cu \
            -o build/ramstream32_direct2d_w${{ matrix.width }}
      - name: Compile lowdesc factorized RAM backend
        shell: bash
        run: |
          nvcc -O3 -std=c++17 -lineinfo -arch=sm_80 \
            -DTARGET_W=${{ matrix.width }} \
            -DLOW_LUT_K=${{ matrix.low }} \
            -DHIGH_LUT_K=${{ matrix.high }} \
            src/cuda/gridfp/oneesan_cuda_gridfp_ramstream32_factorized_lowdesc.cu \
            -o build/ramstream32_lowdesc_w${{ matrix.width }}
      - name: Compile bidesc factorized RAM backend
        shell: bash
        run: |
          nvcc -O3 -std=c++17 -lineinfo -arch=sm_80 \
            -DTARGET_W=${{ matrix.width }} \
            -DLOW_LUT_K=${{ matrix.low }} \
            -DHIGH_LUT_K=${{ matrix.high }} \
            src/cuda/gridfp/oneesan_cuda_gridfp_ramstream32_factorized_bidesc.cu \
            -o build/ramstream32_bidesc_w${{ matrix.width }}
      - name: Validate host descriptor plan
        shell: bash
        run: |
          build/ramstream32_bidesc_w${{ matrix.width }} \
            ${{ matrix.n }} 4294967291 16384 --plan-only
